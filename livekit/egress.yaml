log_level: info

api_key: devkey
api_secret: devsecret
ws_url: ws://livekit:7880

# Health check port (required)
health_port: 9090

# CPU and memory limits
cpu_cost:
  room_composite_cpu_cost: 3.0
  track_composite_cpu_cost: 2.0
  track_cpu_cost: 1.0
